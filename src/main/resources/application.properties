spring.application.name=food-donation-system

# ===============================

# SERVER

# ===============================

server.port=8080

# ===============================

# DATABASE (MySQL)

# ===============================

spring.datasource.url=jdbc:mysql://localhost:3306/food_donation
spring.datasource.username=root
spring.datasource.password=Piyush@123

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

#  REMOVE old dialect

# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ? Either remove it completely (recommended)

# OR use the new generic dialect:

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===============================

# JWT

# ===============================

jwt.secret=secretKey
jwt.expiration=86400000

# ===============================

# LOGGING

# ===============================

#logging.level.org.springframework.security=DEBUG
